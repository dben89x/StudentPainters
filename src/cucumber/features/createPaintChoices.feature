#when running createPaintChoices,
#returns an array of type String - paintChoices
#initializes array of type String with a size that matches the length of the paints array
#extracts name attribute from each Paint in paints, and assigns each Paint name to a corresponding index value
#at each index of paints, value of Paint.name should be equivalent to the value of each corresponding index in paintChoices
Feature: Create Paint Choices

  Scenario: Create Paint Choices With No Errors
    Given 
    When 
    Then 
